// reducers/someReducer.js
const initialState = {
  // Initial state
};

const auth = (state = initialState, action) => {
  switch (action.type) {
    // Handle different actions
    default:
      return state;
  }
};

export default auth;
